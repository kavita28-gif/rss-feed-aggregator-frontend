const initialState = {
    // Add initial state for RSS feed URLs
  };
  
  const feedReducer = (state = initialState, action) => {
    switch (action.type) {
      // Add cases for different feed actions
      default:
        return state;
    }
  };
  
  export default feedReducer;